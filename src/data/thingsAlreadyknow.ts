import type { Things } from "../types/types";
const thingsAlreadyKnow: Things = {
  things: [
    { id: 1, name: "FrontEnd" },
    { id: 2, name: "Lo que hay a la derecha de un igual es una expresion" },
  ],
};

export default thingsAlreadyKnow;
